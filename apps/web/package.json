{
  "name": "@finance-tracker/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "echo 'No lint configured for @finance-tracker/web'",
    "test": "echo 'No tests yet for @finance-tracker/web'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@finance-tracker/shared-types": "workspace:*"
  }
}
