{
  "name": "@finance-tracker/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "echo 'No lint configured for @finance-tracker/api'",
    "test": "echo 'No tests yet for @finance-tracker/api'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@finance-tracker/data-access": "workspace:*",
    "@finance-tracker/domain": "workspace:*",
    "@finance-tracker/shared-types": "workspace:*"
  }
}
