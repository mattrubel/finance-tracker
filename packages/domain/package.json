{
  "name": "@finance-tracker/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "echo 'No lint configured for @finance-tracker/domain'",
    "test": "echo 'No tests yet for @finance-tracker/domain'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@finance-tracker/shared-types": "workspace:*"
  }
}
